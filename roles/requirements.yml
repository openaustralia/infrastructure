---
roles:
  - src: rvm.ruby
    # Switch to latest version as of Jan 2019
    version: v2.1.2

  - src: zzet.rbenv
    # Latest version as of Aug 2024
    # Need to upgrade from 3.4.7 to include the fix
    # https://github.com/zzet/rbenv/commit/8d0f552cf4375688dc302220da280b6cc01c8d25
    version: 3.6.0

  - src: newrelic.newrelic-infra
    version: 0.5.0

  - src: geerlingguy.swap
    # Latest version as of Aug 2026
    version: 2.1.0

# Collections come from the ansible community package (see requirements.txt), which bundles
# curated, version-matched releases of everything we use (amazon.aws, community.aws,
# community.mysql, community.postgresql, community.general, ansible.posix). Don't add a
# collections section that shadows the bundled copies unless a specific version is unavoidable.
