# Note:
# Groups without these suffixes can be both live and new servers
# There is currently no group for servers that are in the process of being retired
#
# Other inventory files:

# KEEP EMPTY - see `Vagrantfile` for development inventory
[development]

# KEEP EMPTY - used by `Vagrantfile` to set up rds equivalents
[mysql]
[postgresql]
[redis]

# INVENTORY IN alphabetical order by group name

# NOTE: The postal server is a Linode instance, NOT ec2 - see terraform/postal
[postal]
postal.oaf.org.au

