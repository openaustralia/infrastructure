pip~=20.0
# Note that ansible 2.10 is currently untested for every role other than
# plausible. For those other roles we currently know it works with ansible 2.9
ansible~=2.10.7
ansible-lint~=5.4.0
boto3
botocore
linkchecker
psycopg2-binary
PyMySQL
virtualenv~=20.34.0
yamllint~=1.38.0
